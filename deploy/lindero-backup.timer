# Nightly backup at 02:15, plus a catch-up run if the machine was off.

[Unit]
Description=Nightly Lindero backup

[Timer]
OnCalendar=*-*-* 02:15:00
Persistent=true
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
